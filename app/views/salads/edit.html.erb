<h1>Salad Edit</h1>
<%= form_for :salad, url: salad_path(@salad.id), method: :patch do |f| %>




<% if @salad.errors.any? %>
    <div id="errorExplanation">
      <h2><%= pluralize(@salad.errors.count, "error") %> prohibited
        this salad from being saved:</h2>
      <ul>
        <% @salad.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
<% end %>

    <li>
      <%= f.label :salad_name %>
      <%= f.text_field :salad_name %>
    </li>
    <li>
      <%= f.label :description %>
      <%= f.text_field :description %>
    </li>
    <li>
      <%= f.label :lettuce_type %>
      <%= f.text_field :lettuce_type %>
    </li>

<p>
  <%= f.submit %>
</p>
<% end %>

<%= link_to 'Back', salads_path %>

<% end %>
